/**
 * スタイルシート用のコンポーネントファイル
 */

import { StyleSheet } from "react-native";

// スタイルを定義する。
const styles = StyleSheet.create({
      container: {
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center',
      },
      counter: {
            fontSize: 20,
      },
});

export default styles;